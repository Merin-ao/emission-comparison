{
  "name": "@emission-comparison/emissions-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Emissions tool server for ZAP — wraps the data-lake noon report (real, token-accessible) + emission-analytics CII/EU-ETS/FuelEU (gated behind RBAC).",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@0north/zap-widgets": "file:../zap-widgets",
    "fastify": "^5.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
